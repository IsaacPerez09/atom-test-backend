name: Deploy Backend

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          
      - run: npm ci
      - run: npm run build
      
      - run: npx firebase deploy --only functions --project atom-task-manager-77028
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}